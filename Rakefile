# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'rack_proxypass' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'rack_proxypassx' # if different than 'rack_proxypass'
end

# vim: syntax=ruby
